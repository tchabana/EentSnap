import React, {useState} from 'react';
import {
  Text,
  View,
  StyleSheet,
  ScrollView,
  StatusBar,
  ImageBackground,
  TouchableOpacity,
  FlatList,
  ToastAndroid,
} from 'react-native';

const SeatBookingScreen = ({navigation, route}: any) => {

  return (
    <View>
        <Text>SeatBookingScreen Screen</Text>
    </View>
  );
};

export default SeatBookingScreen;
